const express = require('express'); const c = require('../controllers/venueController'); const b = require('../controllers/bookingController'); const { requireAuth } = require('../middleware/auth'); const router = express.Router();
router.get('/', c.home); router.get('/dashboard', requireAuth, c.dashboard); router.get('/venues', c.list); router.get('/venues/:id', c.details); router.get('/api/availability', c.availability); router.get('/api/suggestions', c.suggestions); router.post('/venues/:id/favorite', requireAuth, require('../controllers/userController').toggleFavorite); router.post('/bookings', requireAuth, b.create); router.get('/bookings', requireAuth, b.list); router.get('/bookings/:id', requireAuth, b.details); router.get('/bookings/:id/confirm.pdf', requireAuth, b.pdf); router.post('/bookings/:id/cancel', requireAuth, b.cancel); module.exports = router;
